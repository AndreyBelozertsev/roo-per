{% extends 'PortalAdminBundle::adminBase.html.twig' %}

{% block title %}
    {{ parent() }} |
    {{ 'system_mode_form.action_edit'|trans }}
{% endblock %}

{% block pageheader %}
    {{ 'system_mode_form.action_edit'|trans }}
{% endblock %}

{% block topbuttons %}
    <a class="btn btn-success " href="{{ path('admin_admin_system_mode_viewall') }}"><i class="glyphicon glyphicon-repeat"></i>
        {{ 'come_back'|trans }}
    </a>
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        <div id="form-body" class="clearfix">
                {% include 'PortalAdminBundle:SystemModeAdmin:includeForm.html.twig' %}
            <div class="form-group">
                <div class="col-sm-3">
                </div>
                <div class="col-sm-9">
                    <div id="btn_photo_report" class="form-actions">
                        <button id="btn_edit" class="btn btn-success" type="submit" name="btn_edit" ><i class="glyphicon glyphicon-floppy-disk"></i> {{ 'save'|trans }}</button>
                    </div>
                </div>
                <div class="clearfix"></div>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}
